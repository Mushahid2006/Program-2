# Simple Colored Box

This is a basic web page that shows a centered colored box with some text inside.

## 🧠 Features
- Uses **flexbox** to center the box both vertically and horizontally.
- The box has a **solid border**, background color, and centered text.
- Uses **internal CSS** within the `<style>` tag.

## 💻 How to Run
1. Save the code as `box.html`.
2. Open it in any browser.
3. A coral-colored box with the text “Hello Box!” will appear in the center of the page.

## 🧩 Technologies Used
- HTML
- CSS (internal style)
